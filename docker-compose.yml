version: "3.5"
services:

  php:
    build: ./docker/php
    container_name: fib-co-php
    image: fib-co-php
    volumes:
      - ".:/project"
    networks:
      - network-fib

networks:
  network-fib:
    name: network-fib
